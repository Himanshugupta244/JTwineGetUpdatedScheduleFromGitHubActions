# React Patterns

## Note
This project currently uses vanilla JS (no React). This skill is reserved for future frontend migration.

## Guidelines (for future use)
- Functional components with hooks
- Component-per-feature organization
- Props for data down, callbacks for events up
- Use context for shared state, avoid prop drilling
